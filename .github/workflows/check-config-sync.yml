name: Check config sync

on:
  pull_request:
    paths:
      - "ruff.toml"
      - "pyproject.toml"
      - ".github/workflows/check-config-sync.yml"
  push:
    branches:
      - feat/msx-bridge-player-provider

jobs:
  check:
    uses: trudenboy/ma-provider-tools/.github/workflows/reusable-check-config-sync.yml@main
    with:
      domain: msx_bridge
